# Capture: Dealing with files (MDN Web Docs, Learn web development)

- Retrieval URL: https://developer.mozilla.org/en-US/docs/Learn/Getting_started_with_the_web/Dealing_with_files
- Capture date: 2026-09-07
- Capture method: WebFetch (verbatim-extraction prompt) against the live page. Excerpts are exact quotes as returned. Not a full-page archive.
- Publisher: Mozilla (MDN Web Docs)

## Captured excerpts

What an .html file is:

> "In this case, the extension is .html, which means the file should contain plain text, and more specifically, HTML code."

> "This is HTML, the language we use to define and structure web page content."

Opening an HTML file in a browser:

> "Save your HTML file, then load it in your web browser. You can do this by Ctrl/right-clicking the HTML file, then choosing Open With and selecting a web browser from the resulting sub-menu. You could also open your file system UI and a web browser window on the same screen, and drag and drop the HTML file over the top of the web browser window."

Servers:

> "When you're building a website, you need to assemble these files into a sensible structure on your local computer, make sure they can talk to one another, and get all your content looking right before eventually putting them on a server for the world to see."

## Recorded absence

The fetch asked specifically whether a web server is needed to view a local HTML file. The page makes no explicit statement that one is or is not needed. The page's only server references are about publishing a website later. The observable fact that a saved .html file opens in a browser with no server running is testable directly (and is tested by the procedure build-roi-calculator-copilot-free), but it is not asserted by this source in those words.
